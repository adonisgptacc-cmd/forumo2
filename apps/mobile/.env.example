# Set by EAS build profiles (development / preview / production)
APP_ENV=development

# Override API base URL (optional — defaults to env-specific URL in app.config.ts)
# API_BASE_URL=https://api.forumo.app/api/v1

# EAS project ID (required for OTA updates)
EAS_PROJECT_ID=
